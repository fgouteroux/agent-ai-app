apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana
  namespace: agent-ai-app
  labels:
    app: grafana
spec:
  version: "12.4.2"

  config:
    security:
      admin_user: admin
    plugins:
      allow_loading_unsigned_plugins: agent-ai-app

  deployment:
    spec:
      template:
        spec:
          containers:
            - name: grafana
              env:
                - name: GF_SECURITY_ADMIN_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: grafana-admin
                      key: password
                - name: GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS
                  value: agent-ai-app
                - name: GF_INSTALL_PLUGINS
                  # REPLACE_ME: point this at your own release asset once
                  # published (e.g. a GitHub Releases download URL, or an
                  # internal artifact host) -- this repo doesn't host one.
                  value: "https://github.com/REPLACE_ME/agent-ai-app/releases/latest/download/agent-ai-app-linux-amd64.zip;agent-ai-app"
              volumeMounts:
                - name: llm-plugin-config
                  mountPath: /etc/grafana/provisioning/plugins
                  readOnly: true
                - name: sa-token
                  mountPath: /var/run/secrets/grafana-sa
                  readOnly: true
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  cpu: 500m
                  memory: 512Mi
          volumes:
            - name: llm-plugin-config
              secret:
                secretName: llm-plugin-config
            - name: sa-token
              secret:
                secretName: llm-sa-token
                optional: true

  ingress:
    metadata:
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        kubernetes.io/tls-acme: "true"
    spec:
      ingressClassName: nginx
      rules:
        - host: grafana.example.com
          http:
            paths:
              - path: /
                pathType: Prefix
                backend:
                  service:
                    name: grafana-service
                    port:
                      number: 3000
      tls:
        - hosts:
            - grafana.example.com
          secretName: grafana-tls
